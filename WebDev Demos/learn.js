// var, let, const
// constant doesn't change while let can be change, try to mostly use const
// String, Numbers, Boolean, null, undefined

const name = 'John';
const age = 30;

// Template String
const hello = `My name is ${name} and I am ${age}`;

//console.log(s.split(', '));

const num = [1,2,2,'abe'];
console.log(num);